#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='mltools',
    description='Suite of scripts for personal use',
    version='0.0.1',
    autor='Marc Roig',
    packages=find_packages()
)